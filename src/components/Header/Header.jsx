import './Header.css';
// Fun header
function Header () {
    return (
        <header className="App-header">
          <h1 className="App-title">Gallery of My Life</h1>
        </header> 
    ) // classes on everything. never know when you might need 'em
}

export default Header;